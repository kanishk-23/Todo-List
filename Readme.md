# for Backend:-
npm init -y
npm install cors mongodb mongoose express dotenv nodemon
npm i jsonwebtoken validator
npm run dev
now design the model then design how controller works and then on routes now connect mongoDB and in index


# for Frontend:-
npm create vite@latest frontend -- --template react
cd frontend
npm i axios
npm run dev
now design the frontend and work on authentication and connect with backend.


# Environment variables
frontend:- VITE_BASE_URL 
        
backend:- PORT, MONGO_URI, FRONTEND, SECRET
